<h1> <%= @category_page.name %> </h1>

<p>
<% if last_parsed_result = @category_page.last_parsed_result %>
  Last parsed date was: <%= last_parsed_result.date %>
  <%= link_to 'See parsing results', admin_category_parsing_result_path(@category_page.last_parsed_result) %>  
<% else %>
  Page has not been parsed yet 
<% end %>
